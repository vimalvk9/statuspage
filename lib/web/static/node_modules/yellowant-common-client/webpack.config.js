function buildConfig(env) {
  if(env == "prod" && !process.env.S3BUCKETPATH) {
    console.error("!!! ERROR: set the S3 Bucket Path env variable: S3BUCKETPATH !!!");
    return;
  }
  return require('./config/webpack.' + env + '.js')(env)
}

module.exports = buildConfig;