import template from './team-switcher.html';
import { TeamSwitcherController as controller } from './team-switcher.controller';
import './team-switcher.scss';

controller.$inject = ["UserApi", "$window", "$location"];

export let TeamSwitcherComponent = {
  template,
  controller,
  bindings : {
    appendUrl: "@",
    currentTeamDomain: "="
  }
};