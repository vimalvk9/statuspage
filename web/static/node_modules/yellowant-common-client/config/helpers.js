const path = require('path');

const rootPath = path.resolve(__dirname, '..');
const distPath = path.resolve(rootPath, 'dist');
const publicPath = 'http://localhost:3000/bundle/';
const srcPath = path.resolve(rootPath, 'src');
const stylePath = path.resolve(srcPath, 'style');

const angularPath = path.resolve(srcPath, "angularjs");
const reactPath = path.resolve(srcPath, "react");


module.exports = {
  rootPath,
  distPath,
  srcPath,
  stylePath,
  angularPath,
  reactPath
}