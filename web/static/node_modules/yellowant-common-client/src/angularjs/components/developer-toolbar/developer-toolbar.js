import angular from 'angular';
import angularMaterial from 'angular-material';

import { DeveloperToolbarComponent } from './developer-toolbar.component';

import { angularMaterialConfig } from "../../config/angular-material.config";
import { teamSwitcher } from "../team-switcher/team-switcher";
import { userApi } from "../../api/user.api";

export let developerToolbar = angular
  .module('developerToolbar', [
    angularMaterial,
    userApi,
    teamSwitcher,
    angularMaterialConfig
  ])
  .component('developerToolbar', DeveloperToolbarComponent)
  .name;