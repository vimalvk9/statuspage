import template from './marketplace-toolbar.html';
import { MarketplaceToolbarController as controller } from './marketplace-toolbar.controller';
import './marketplace-toolbar.scss';

controller.$inject = ["$location"];

export let MarketplaceToolbarComponent = {
  template,
  controller,
  bindings: {
    toolbarClass: "@",
    appendUrl: "@",
    showBrowseButton: "<"
  }
};